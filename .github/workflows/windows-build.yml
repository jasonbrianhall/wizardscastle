name: Qt5 Windows Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v2

    - name: Install Qt
      uses: jurplel/install-qt-action@v3
      with:
        version: '5.15.2'
        host: 'windows'
        target: 'desktop'
        arch: 'win64_msvc2019_64'
        tools: 'tools_ifw tools_qtcreator,qt.tools.qtcreator'
        cache: true

    - name: Setup MSVC
      uses: ilammy/msvc-dev-cmd@v1

    - name: Build application
      run: |
        cd c; qmake -f wizard_castle.pro
        nmake

    - name: Run windeployqt
      run: |
        windeployqt.exe your_app.exe

    - name: Package application
      run: |
        mkdir win_package
        move your_app.exe win_package\
        move *.dll win_package\
        move platforms win_package\
        move styles win_package\
        # Add any additional files your application needs
        Compress-Archive -Path win_package -DestinationPath win_package.zip

    - name: Upload artifact
      uses: actions/upload-artifact@v2
      with:
        name: windows-app
        path: win_package.zip
